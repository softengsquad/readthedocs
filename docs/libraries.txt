Day Library
============
Day Class:
------------
holds information about the sessions that are occuring this day
- stores the date in a _DateTime_ Format
- stores a _List_ of sessions (in _session_ format)

constructors
------------
Day(DateTime date,
    List<Session>sessions
)

Properties
----------
date <--> DateTime
(getter/setter pair)

hashCode -> int
the hash code for this object
(no setter) (inherited)

runtimeType -> Type
(no setter) (inherited)
a representation of the runtime type of the object


sessions <--> List<Session>
(getter/setter pair)

weekDay <--> String
(getter/setter pair)

Methods
-------
getSchedule() -> List<String>
Returns a List Of session Strings

noSuchMethod(Invocation invocation) -> dynamic
(inherited)
Invoked when a nonexistent method or property is accessed.

toString() -> String
(inherited)
A string representation of this object.

Operators
---------
operator ==(Object other) -> bool
(inherited)
The equality operator.









google_oauth_api library
========================

constants
---------
clientId -> const String
The OAuth 2.0 Client ID given by Google Cloud.
"354034818654-ngj5kfhaq8n4j4ou3ok265t0q37c4a8h.apps.googleusercontent.com"

scopes -> const List<String>
The list of scopes that will be granted to the application once the user authenticates with Google. This must align with the scopes defined in the OAuth settings in the Google Cloud API Console.
<String>["email", "https://www.googleapis.com/auth/calendar.readonly", "https://www.googleapis.com/auth/calendar.events.owned.readonly&qu…






main
====
classes
-------
myApp
-----
MyApp({Key? key})
const

properties
----------
hashCode -> int
(no setter) (inherited)
The hash code for this object.

key -> Key?
(final) (inherited)
Controls how one widget replaces another widget in the tree.

runtimeType -> Type
(no setter) (inherited)
A representation of the runtime type of the object

methods
-------





MyHomePage
----------

functions
---------
main() -> void

